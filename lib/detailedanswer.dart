import 'package:flutter/material.dart';

class Detailedanswer extends StatelessWidget {
  const Detailedanswer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}